<UserControl
    x:Class="AdactaInternational.AdactaReportsShoppingBag.Desktop.Controls.DoubleTextboxControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AdactaInternational.AdactaReportsShoppingBag.Desktop.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" Spacing="12">
        <TextBox x:Name="FirstTextBox"
             Header="{x:Bind FirstLabel, Mode=OneTime}"
             Text="{x:Bind FirstValue, Mode=TwoWay}" />

        <TextBox x:Name="SecondTextBox"
             Header="{x:Bind SecondLabel, Mode=OneTime}"
             Text="{x:Bind SecondValue, Mode=TwoWay}" />
    </StackPanel>
</UserControl>
