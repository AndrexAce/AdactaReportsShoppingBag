<UserControl
    x:Class="AdactaInternational.AdactaReportsShoppingBag.Desktop.Controls.PenelopeCredentialsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" Spacing="12">

        <StackPanel Orientation="Vertical" Spacing="4">
            <TextBox Header="Nome utente"
                     Text="{x:Bind _viewModel.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="{x:Bind _viewModel.UsernameError, Mode=OneWay}"
                       Visibility="{x:Bind _viewModel.UsernameErrorVisibility, Mode=OneWay}"
                       Foreground="Red" />
        </StackPanel>

        <PasswordBox Header="Password"
                 Password="{x:Bind _viewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="{x:Bind _viewModel.PasswordError, Mode=OneWay}"
                   Visibility="{x:Bind _viewModel.PasswordErrorVisibility, Mode=OneWay}"
                   Foreground="Red" />
    </StackPanel>
</UserControl>