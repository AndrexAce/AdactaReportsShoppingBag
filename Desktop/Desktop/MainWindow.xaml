<Window
    x:Class="AdactaInternational.AdactaReportsShoppingBag.Desktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="AdactaReports Shopping Bag">
    
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="TitleBar" Grid.Row="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
            <Image x:Name="TitleBarIcon"
               HorizontalAlignment="Left"
               Width="18" Height="18"
               Margin="8,0,0,0"/>

            <TextBlock x:Name="TitleTextBlock" FontSize="12"/>
        </StackPanel>

        <muxc:MenuBar Grid.Row="1">
            <muxc:MenuBarItem x:Uid="/MainWindow/ProjectMenuBarItem">
                <MenuFlyoutItem x:Uid="/MainWindow/NewProjectMenuFlyoutItem" Icon="Add">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="N" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Uid="/MainWindow/OpenProjectMenuFlyoutItem" Icon="Import">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="O" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="SaveProjectMenuFlyoutItem" x:Uid="/MainWindow/SaveProjectMenuFlyoutItem" Icon="Save" Visibility="Collapsed">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="S" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
            </muxc:MenuBarItem>

            <muxc:MenuBarItem x:Uid="/MainWindow/HelpMenuBarItem">
                <MenuFlyoutItem x:Uid="/MainWindow/AboutMenuFlyoutItem" Icon="Help">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="H" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
            </muxc:MenuBarItem>
        </muxc:MenuBar>

        <Frame x:Name="RootFrame" Grid.Row="2" Padding="8" CornerRadius="8,8,0,0" VerticalAlignment="Stretch" Background="{StaticResource LayerFillColorDefaultBrush}" />
    </Grid>
</Window>